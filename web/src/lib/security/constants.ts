/** Max lengths for user-editable text fields (client-side guardrails). */
export const MAX_RECORD_ID_LENGTH = 128;
export const MAX_SHOP_NAME_LENGTH = 256;
export const MAX_EQUIPMENT_LABEL_LENGTH = 128;
export const MAX_NOTES_LENGTH = 4000;
/** Max JSON payload size for verify paste/upload (256 KB). */
export const MAX_JSON_TEXT_CHARS = 256 * 1024;
export const API_REQUEST_TIMEOUT_MS = 30_000;
