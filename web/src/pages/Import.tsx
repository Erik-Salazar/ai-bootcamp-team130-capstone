// Import (`/import`) — spec §13.5
// TODO(Frontend): textarea for mock webhook JSON, preview normalized record
// before submit, then reuse submit flow.
export default function Import() {
  return (
    <section>
      <h2>Import Mock Webhook JSON</h2>
      <p>Import UI will go here.</p>
    </section>
  );
}
