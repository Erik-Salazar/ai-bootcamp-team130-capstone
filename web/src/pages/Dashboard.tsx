// Dashboard (`/`) — spec §13.2
// TODO(Frontend): table of records with VIN/status filters, links to detail
// and verify, Retry button for anchor_failed rows.
export default function Dashboard() {
  return (
    <section>
      <h2>Dashboard</h2>
      <p>Record list, filters, and status will go here.</p>
    </section>
  );
}
