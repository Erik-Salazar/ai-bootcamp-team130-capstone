// Submit Record (`/submit`) — spec §13.1
// TODO(Frontend): form matching canonical schema, inline validation errors,
// success -> redirect to record detail / show receipt link.
export default function Submit() {
  return (
    <section>
      <h2>Submit Record</h2>
      <p>Submission form will go here.</p>
    </section>
  );
}
